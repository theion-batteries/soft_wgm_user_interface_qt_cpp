name: Docker Image CI

on:
  push:
    branches: [ "dev_win_api" ]
  pull_request:
    branches: [ "dev_win_api" ]

jobs:

  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build and Push the Docker image
      run: .\docker_push_qt_win_img.bat
