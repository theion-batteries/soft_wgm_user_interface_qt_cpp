name: Docker Build Image CI QT

on:
  push:
    branches: [ "dev_build_docker_img" ]
  pull_request:
    branches: [ "dev_build_docker_img" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build -t windows_qt6_image .

